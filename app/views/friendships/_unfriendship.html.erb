<% if current_user != @user %> 
  <%= form_with(model: @friendship, url: friendship_path(current_user.friendships.where(invited_friend_id: @user.id).ids), method: :delete ) do |f| %>
    <%= f.submit "Delete Invitation", data: { "disable-with": "Saving..." } %>
  <% end %>
<% end %>